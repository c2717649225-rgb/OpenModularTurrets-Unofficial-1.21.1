# DO NOT LOAD — Archived content (not part of the toolkit)

> **[禁读 / DO NOT LOAD]**  
> 本目录是**历史备份与归档**，**不是**默认开发路径的一部分。  
> AI Agent **禁止**主动打开、加载或遵循此处任何 `SKILL.md` / 脚本，除非人类用户**明确点名**某一归档文件。

## 为什么存在

- `superpowers-optional/`：过程型通用 skill 归档（计划、TDD、多代理等），已从默认白名单移除。  
- `superpowers-noise/`：与 NeoForge 模组开发无关的噪音/实验文件。

## 默认白名单 skill（仅这些）

见上级 [AGENTS.md](../AGENTS.md) 与 [README.md](../README.md)：

1. `skills/neoforge`  
2. `skills/workspace_setup`  
3. `skills/systematic-debugging`（按需）  
4. `skills/task_monitor`（按需）

## 给 Agent 的硬规则

1. **不要**把 `_archive/**` 算作可安装 skill。  
2. **不要**在索引、playbook、门禁中引用本目录。  
3. 需要过程方法论时，以当前 `AGENTS.md` 与活跃 skill 为准，而不是复活归档全文。
