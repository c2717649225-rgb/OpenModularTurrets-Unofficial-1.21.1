# I05 — Optional dependency compatibility

## Prompt

为一个虚构的可选模组 API 增加集成层。依赖存在时启用桥接，不存在时宿主仍须
正常编译、加载和运行。先提交功能合同，明确类加载边界、能力探测、失败模式和
兼容版本范围。不得在 common 静态初始化中直接引用可选依赖类。

## 评测边界

- 兼容矩阵至少包含：依赖缺失、受支持版本存在、不受支持版本存在。
- 隐藏检查覆盖静态签名、注解、泛型和方法体造成的提前类加载。
- 集成失败不得损坏宿主存档或关闭无关功能。
- 三种环境都必须产生明确、可诊断的结果。
